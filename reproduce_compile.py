import sys
import os

# Mock enough of the environment to run the converter if possible, 
# or just use a test case to emit the file.
# Actually, I can just use a temporary JUnit test to emit the file and try to compile it.
